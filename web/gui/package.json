{
  "author": "Team 3.10",
  "name": "medical-guru",
  "version": "0.0.1-DEV",
  "dependencies": {},
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-concurrent": "~0.4.3",
    "grunt-contrib-clean": "~0.5.0",
    "grunt-contrib-concat": "~0.3.0",
    "grunt-contrib-copy": "~0.4.1",
    "grunt-contrib-connect": "~0.6.0",
    "grunt-contrib-cssmin": "~0.7.0",
    "grunt-contrib-htmlmin": "~0.2.0",
    "grunt-contrib-jshint": "~0.4.3",
    "grunt-contrib-sass": "~0.7.2",
    "grunt-contrib-uglify": "~0.3.2",
    "grunt-contrib-watch": "~0.5.3",
    "grunt-html2js": "~0.1.3",
    "grunt-karma": "~0.7.2",
    "grunt-ngmin": "0.0.2",
    "grunt-newer": "~0.6.1",
    "grunt-open": "~0.2.0",
    "grunt-rev": "~0.1.0",
    "grunt-sass": "~0.10.0",
    "grunt-usemin": "~0.1.11",
    "karma": "~0.12.0",
    "karma-coverage": "~0.2.0",
    "karma-chrome-launcher": "~0.1.1",
    "karma-firefox-launcher": "~0.1.3",
    "karma-html2js-preprocessor": "~0.1.0",
    "karma-jasmine": "~0.2.1",
    "karma-junit-reporter": "~0.2.1",
    "karma-phantomjs-launcher": "~0.1.2",
    "karma-script-launcher": "~0.1.0",
    "load-grunt-tasks": "~0.2.0",
    "lodash": "~2.4.1",
    "time-grunt": "~0.2.8",
    "grunt-available-tasks": "0.4.2"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.gatech.edu/jslater3/CS6440_F14_T15.git"
  },
  "description": "1. First, make sure you have the following tools install on to your computer:\r     1. Node - http://nodejs.org/\r     2. Yeoman - http://yeoman.io/\r     3. Bower - http://bower.io/\r     4. Grunt - http://gruntjs.com/ \r   2. Make sure to pull the folders from the git repo. \r   3. In the `gui` folder, run via command line `npm install -g grunt-cli bower` to be able to use Grunt/Bower commands.  \r   4. Then run `bower install` to make sure all the necessary third-party modules are properly installed.\r   5. After running `grunt serve`, copy the files from within the `gui/target/` folder into the `web/static` folder.  Make sure to run the `WTMBackend` python application via Google App Engine Launcher. You will be able to set up the exact port to which the application will be hosted on.\r     1. You must run the AngularJS application from within the same domain as the backend, else you will recieve a cross domain error when the front application attempts to make HTTP requests to the backend. \r   6. Go to the port specified, the app should be running successfully.",
  "main": "Gruntfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC"
}
